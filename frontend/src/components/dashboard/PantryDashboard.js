import React from 'react';
import { Box, Typography } from '@mui/material';

const PantryDashboard = () => {
    return (
        <Box>
            <Typography variant="h4" gutterBottom>
                Pantry Dashboard
            </Typography>
            {/* Add pantry dashboard content */}
        </Box>
    );
};

export default PantryDashboard; 